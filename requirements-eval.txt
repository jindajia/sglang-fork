# Build dependencies (pre-installed separately before this file)
# grpcio-tools, numpy, packaging are installed first in the script

# simple_math evaluator dependencies (not in tore-eval's setup)
ray
word2number
huggingface-hub>=0.25.0

# KV rotation / kmeans (must use jindajia's fork, PyPI version lacks _euclid_iter_compiled)
# flash-kmeans is installed separately in setup_env.sh via git+https://github.com/jindajia/flash-kmeans

# lm_eval (used in kmeansworkflow.sh for Stage 1 KV cache dumping)
lm_eval

# Note: fast-hadamard-transform is installed after sglang (requires torch)
